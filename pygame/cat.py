import pygame
class Cat:
    def __init__(self, colour, direction, x, y, size = 20):
        self.colour = colour
        self.direction = direction
        self.x = x
        self.y = y
        self.size = size